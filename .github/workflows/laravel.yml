name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Install Composer Dependencies
      run: composer install --no-dev --prefer-dist --no-progress --no-suggest

    - name: Run Tests
      run: php artisan test

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Execute deployment script
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      run: |
        echo "$SSH_PRIVATE_KEY" > private_key
        chmod 600 private_key
        ssh -i private_key -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_IP_ADDRESS }} 'bash -s' < ./deploy.sh


# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout the code
#       uses: actions/checkout@v4
    
#     - name: Deploy to ssh server
#       uses: easingthemes/ssh-deploy@v2.1.5
#       env:
#         SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#         REMOTE_HOST: ${{ secrets.SSH_IP_ADDRESS }}
#         REMOTE_USER: ${{ secrets.SSH_USERNAME }}
#         REMOTE_PORT: ${{ secrets.SSH_PORT }}
#         TARGET: ${{ secrets.SSH_PROJECT_PATH }}
#         SSH_CMD_ARGS : "-o StrictHostKeyChecking=no"

#     - name: executing remote ssh commands using password
#       uses: appleboy/ssh-action@v0.1.10
#       with:
#         host: ${{ secrets.SSH_IP_ADDRESS }}
#         username: ${{ secrets.SSH_USERNAME }}
#         password: ${{ secrets.SSH_PASSWORD }}
#         port : ${{ secrets.SSH_PORT }}
#         script: "cd ${{ secrets.SSH_PROJECT_PATH }} && sh ./deploy.sh"
  